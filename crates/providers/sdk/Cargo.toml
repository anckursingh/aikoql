[package]
name = "aikoql-provider-sdk"
description = "Provider SDK for aikoql — trait-based interface for external data system connectors"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
aikoql-kernel = { path = "../../kernel" }
