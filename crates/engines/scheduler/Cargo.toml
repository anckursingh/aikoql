[package]
name = "aikoql-scheduler"
description = "Aikoql Scheduler Engine: background index maintenance, KE-driven async jobs. Services around the kernel, never on the commit path."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
aikoql-kernel = { path = "../../kernel" }
aikoql-storage-v2 = { path = "../../storage/aikoql-v2" } # checkpoint crash-child harness (bin + tests)
aikoql-vector = { path = "../../engines/vector" } # production HNSW/Tantivy in the crash matrix
