[package]
name = "aikoql-reasoning"
description = "Aikoql Reasoning Engine: rule execution over Knowledge Objects. Consumes KEs, fires rules, writes provenance-tagged claims."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
aikoql-kernel = { path = "../../kernel" }
aikoql-scheduler = { path = "../../engines/scheduler" }
