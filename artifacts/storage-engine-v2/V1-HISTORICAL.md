# V1 Historical Numbers — Frozen (S-05)

The adoption-era record of the decommissioned v1/redb engines (S-02 removed
the code; the harness went v2-only in the same sweep). These artifacts are
**frozen historical numbers**: no suite regenerates them, and no live gate,
baseline, or document cites them as current evidence.

- `result-1m-aikoql.json` / `workloads-1m-aikoql.md` — v1 engine at 1M (M28 chain)
- `result-1m-redb.json` / `workloads-1m-redb.md` — redb at 1M
- `result-1m-memory.json` / `workloads-1m-memory.md` — in-RAM reference at 1M
  (same v1-era harness run; the v2 memory rows come from fresh runs in
  `result.json` / `workloads.md`)

The adoption verdict and its evidence trail (2026-09-01..05) stand in
`adoption-decision.md` and the bannered HISTORICAL plan docs. The live
baselines are `result.json` (100K) and `result-1m-aikoql-v2.json` (1M),
regenerated by the storage self-regression suite (S-03, gate 5).

Do not extend this list; the v1 record is closed. New numbers are current
evidence and belong in the live baseline files.
