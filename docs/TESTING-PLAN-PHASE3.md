# AIKOQL Phase 3 — Testing Plan

Mirror of `docs/TESTING-PLAN.md` §13.2 for phase 3, same ledger discipline as `docs/TESTING-PLAN-V2.md`: one row per milestone; status flips to ✅ only with real evidence (test names + green counts + artifacts). Requirement numbering continues V2 (S52 was the v2 acceptance matrix — phase 3 starts at §53). Phase 4 rows (P4-M1..M7) carry the class-by-class review's TDD ids (2026-09-10, main@dbd8db4) — no spec sections are numbered past §73. TDD rules below are binding for every milestone.

| # | Milestone | §§ | Status | Evidence |
| --- | --- | --- | --- | --- |
| P3-M0 | Estate hygiene | — | ⬜ | clb001–002; `cargo test --workspace` green; zero artifact diffs on a full local run |
| P3-M1 | Security hardening | §53–55 | ✅ | auth001–008 + auth_surface 2/2 spawn (exit-2 fail-closed pins); `cargo test -p aikoql-mcp` 126/126 (mcp_stdio + connector_certification + auth_surface green); fmt + clippy `-D warnings` green; grep pin — admin/admin prefills+hints removed from graph_ui/studio, only auth001's negative assertion remains; hash-password CLI smoke `$argon2id$`; docker health endpoint allowlisted (CI-covered) |
| P3-M2 | Observability + StorageAdmin | §56–57 | ✅ | met001–003 3/3 (0.09s); met004–006 green in `cargo test -p aikoql-mcp` (74/74 main binary, 129 across binaries); `cargo test -p aikoql-storage-v2` 264/0; fmt + clippy `-D warnings` green; met007 (P3M2_ATTRIB=1, release) instrumentation share **0.02%** ≤1% (16 B put 832 µs/op, 1400 B 902 µs/op, marginal 194.6 ns/op; fsync_count 200000 pins one fsync/Sync-put) — artifact `artifacts/storage-engine-v2/write-stats-overhead.md` |
| P3-M3 | Engine-native snapshot/restore | §58–60 | ✅ | bkp001–006 7/7; REC-002 green (mvp_rec_002 on the native path); 100K cell 0.045 s (3.1 MB/7 files, O(disk), no decode) |
| P3-M4 | Compiler completion | §61–63 | ✅ | cpl001–006 green (cpl_compile 4/4, cpl_execution 2/2 — diamond closure {b,c,d,e}@DEPTH 3, {b,c}@default); cpl007 = golden 13/13 + grammar_coverage 42/42 + fuzz 4/4 rows; parser lib 77/77; runtime 23/0; kernel + mcp suites untouched green; fmt + clippy `-D warnings` green; dogfood INGEST round-trip through repo-built plugin (aikoql tool → aikoql:document → document_list); follow-up fix: `:` in idents — `MATCH aikoql:document RETURN *` green end-to-end through the plugin |
| P3-M5 | Constraint engine (a/b/c) | §64–66 | ✅ | cst001–007 7/7 in `constraint_modes.rs` (mode matrix, severity catalog, cardinality, cross-type, temporal, incremental counters, DISABLED zero-overhead); full kernel suite green (242 unit + transactions 28/28 + qa2_schema 4/4 + conformance 107/107 + fuzz_codec 5/5); `cargo test -p aikoql-mcp` 130/0; fmt + clippy `-D warnings` green; codec v2 + v1-fallback pins (`schema_round_trip_full_and_canonical`, `schema_v1_rows_fall_back_with_defaults`); `docs/MRFC-0060-COVERAGE.md` committed (implemented/prior/descoped per §); dogfood (follow-up): `register_schema` + event-ring loop through the repo-built plugin — advisory remember succeeds, enforced-unique duplicate blocked, diagnostics events carry mode/severity/koid (`mcp-constraint-diagnostics-smoke.mjs` GREEN); `cargo test -p aikoql-mcp` 131/0 |
| P3-M6 | Scale-1M re-run | §67 | ✅ | Fresh 4-backend chain (git 75391b8, release, seed 2555904) all legs rc=0 (v1 1h33m, v2 2h55m, redb 4h05m, memory 61s; ~7 h wall; power plan auto-restored after); artifacts result-1m-{aikoql,aikoql-v2,redb,memory}.json + workloads-1m-*.md committed via report gating; **gate 5 at 1M: W1 58.9/7.4 = 7.96×, W2 41.8/7.2 = 5.81× v1 — both ≤8× PASS** (W1 margin 0.04×); identity divergence 0 — harness shape asserts (zero-loss, per-backend parity) held on all four legs; cells: RSS 386.5 MB vs v1 6334 MB, disk 3.05 vs 4.55 GB, seed 2895 vs 2324 s; 1M gate-5 section in adoption-decision.md (single-backend filtered runs leave `verdict: null` by design — cross-leg ratio documented) |
| P3-M7 | Class-B async | §68–69 | ⬜ | cb001–005; MRFC-0011 §11 conformance green |
| P3-M8 | Background compaction | §70–71 | ⬜ | bgc001–005; CP-001..010 + relocation + crash windows green in background mode; W8 tail cell |
| P3-M9 | SDK & proxy decision | §72–73 | ⬜ | decision doc; kept-surface REDs (sdk001–002) or deletion commits + DAG green |
| P4-M1 | Planner semantic safety (P0) | TDD-COMP-002/003 | ✅ | RED captured pre-implementation: ppl001–003 + the corrected pinning test fail for the stated reason ("left: 1, right: 2" — the second Scan removed; "left: 3, right: 4" — ops dropped), ppl004–006 green; second RED: ppl005 re-coded the original unsafe cross-op dedup (Filter between identical scans is dead code — dedup would make it live), flipped to MUST-NOT-dedup-across-ops per the review's predicate-algebra clause, then green. Fix: equivalence = full tuple (type, subject, roles, tenant) + consecutive-adjacency only; full destructure — a new Scan field becomes a compile error in `dedup_scans` (snapshot/scope don't exist as Scan fields on this branch; the destructure is the guard). TDD-COMP-003 ordering: Filter,Search → Search,Filter IS the implemented semantics (Search-then-Filter, recall-preserving) — module + fn docs corrected, ppl006 pins it. Compiler crate 146/0 (lib 83, cpl_compile 4, fuzz 4, golden 13, grammar_coverage 42); kernel suites untouched-green (36 targets 655 passed 0 failed incl. 243 unit + conformance 107); `cargo test -p aikoql-mcp` 131/0 (75+2+20+8+24+2); fmt + clippy `-D warnings` green |
| P4-M2 | PhysicalHandle + batch resolver | TDD-ID-001/002 | ✅ | RED captured pre-implementation as a compile error (phy001–004 could not build — `PhysicalHandle`/`PhysicalResolver`/`HandleRegistry`/`PlacementSource`/`FormatError::Stale` absent). Shipped: monotonic-counter `PhysicalHandle`; `HandleRegistry<S: PlacementSource>` with `resolve_many` (elementwise, mandatory) and `location(handle)` re-validating the LIVE source every call (superseded generation → `FormatError::Stale`, never old data); shared form `PhysicalResolver for Mutex<HandleRegistry<S>>`; Db-backed source. phy001–005 5/5 (stale-generation fail-closed; resolve_many == resolve; atomic relocation batch old-or-new never mixed; 1M oracle zero divergence — PHY_NIGHTLY=1m, 9.91 s, batch==scalar elementwise + every handle resolves to its own location; Db integration — compaction flips handles not ReplicaIds, pre-relocation handles fail closed); kernel grep pin clean (no PhysicalLocation/SegmentId/BlockId in crates/kernel/src); v2 suite 276/0 across 59 targets (271 + 5 new); fmt + clippy `-D warnings` green |
| P4-M3 | Storage invariant validator | TDD-STOR-005 | ✅ | Behavioral RED captured against a no-op stub (iv001/002/003/004 failed for the stated reasons — flipped key range, size mismatch and duplicate id opened fine today; missing file was Io not the asserted class); then green. `validate_manifest` at open (generation > 0, unique ids, level ≤ 1, key/seq order, record_count > 0, file present + recorded size) + manifest-record ↔ segment-header agreement in the open loop + debug pre-publish at both manifest sites (flush/compact). Class correction caught by the pre-existing pin: `db_recovery::missing_segment_fails_closed` pins Io for a missing file (filesystem condition, not metadata damage) — validator now emits Io for missing files, Corrupt for size/order/dup disagreement; iv002 pins Io. iv001–005 5/5 (0.16 s); v2 suite 281/0 across 60 targets (276 + 5 new); kernel crates untouched (zero files); fmt + clippy `-D warnings` green |
| P4-M4 | Repository complexity audit + temporal/event seek | TDD-KERNEL-001/002, TDD-EVENT-001 | ✅ | Two-phase RED: ker001 ordering-only (BTreeMap sorts the manifest — 41-method sets match, compared as sorted sets); then the env-gated sweep cell ker002 caught a real O(N) before ship — `scan_rev_from` materialized the entire version history for a one-row read (get_version_at 265µs@10 → 3.99s@1M, linear). Fix: `predecessor(prefix, at)` one-step seek — redb `(prefix..=at).rev().next()` (both bounds seeked, O(log n)), MemoryEngine BTreeMap `next_back` + inverted-range guard (std panics where redb doesn't), trait default scan+filter+next_back; `scan_rev_from` deleted. Cell re-run flat: 120.5µs@10 → 430.5µs@10M versions (3.6× across 6 decades, no growth term — 861s cell, mostly the 10M write); ker003 last-100 in a 1M-event journal = 895.1µs (write 61.9s). Boundary pins in both engine test modules (other-koid rows both range bounds, exact hit, absent predecessor, inverted range). ker001–004 4/4; kernel 31 targets 530/0 (524 + 6 new); mcp 131/0; fmt + clippy `-D warnings` green |
| P4-M5 | Read attribution → batch read wave | TDD-READ-001..004, TDD-REL-001, TDD-OBJ-001, TDD-GRAPH-001 | ✅ | Boundary (a) shipped (2026-09-11, no-push): RED = compile error on the missing surface (`ReadTraceRecord`, `Config.trace_every`, `trace_len`/`drain_trace`). Per-request trace as delta-of-aggregates in the `get`/`get_many` wrappers only (zero changes to `get_inner`); `trace_every: u64` (0 = off default — disabled path is the pre-M5 body, one plain load); records value-opaque, seq-unique, bounded 65 536-then-drop; `trace_len`/`drain_trace`. rd001–003 3/3 (same-block N keys → exactly 1 block read; batch == scalar elementwise incl. tombstone/absent; rate-1 records per request, cold→warm cache_hit flip, disabled = 0 records). Boundaries (b)–(d) NOT shipped — evidence-gated (rule 11): M25 falsified naive `get_many` at 100K warm; type-scan/context and graph-frontier batch paths ship only with their own gain cells (M6 1M cells are the decision input); rd004/rd005 are written with those boundaries. v2 suite 284/0 across 61 targets (281 + 3 new); kernel crates untouched; fmt + clippy `-D warnings` green; dogfood: plugin round-trip dev_log 14→15 rows |
| P4-M6 | Streaming checkpoint writer | TDD-ID-003 | ✅ | RED = compile error on the missing surface (`publish_staged_streamed`). `write_streamed` = the ONE writer both paths use (`encode()` delegates — byte-drift impossible); `publish_staged_streamed` reuses the M40 staging protocol (temp write → crash parks → fsync → rename), Db's `write_checkpoint` switched to it. cps001: streamed == materialized == `encode()` byte-for-byte, decodes equal; cps002: torn SECOND publish (park armed post-first-checkpoint — the spawn-armed park would have torn the FIRST, caught in-flight) leaves gen-2 checkpoint authoritative, torn temp ignored at open, state converges; cps003 (CPS_NIGHTLY=1, 58 s): n=1M, encode 81 000 034 B, materialized peak 349.0 MB vs streamed 267.9 MB — the 81.0 MB delta is exactly the encode buffer. ckp001–008 14/14 green against the switched path; v2 suite 287/0 across 62 targets (284 + 3 new); kernel crates untouched; fmt + clippy `-D warnings` green; dogfood dev_log 15→16 rows |
| P4-M7 | Production hardening batch | TDD-RUNTIME-001, TDD-INDEX-001, TDD-VECTOR-001/002, TDD-TIME-001, TDD-COMP-001, TDD-STOR-006 | ⬜ | run001 (bounded overload + measurable p99), idx001 (lag fields on every index), vec001/002 (upsert_many parity; dead-ratio rebuild), time001 (6 HLC property groups), comp001 (O(1) ontology lookup pin), stor006 (byte API cannot answer object reads — type-level pin) |

## Rules carried from v2 (binding) + phase-3 additions

1. **RED first.** Write the failing test, run it, verify it fails for the stated reason, then implement. No implementation before the RED is on the branch. Never weaken an assertion to make one green — weakening is a RED on the review.
2. **Correctness and performance separated.** Correctness pins run on every PR. Measurement cells are env-gated (`*_NIGHTLY`, `P3M2_ATTRIB`, `AIKOQL_REPORT_WRITE`) and reported, not asserted — except the standing asserts (gate 5 ≤8×, zero-loss, identity divergence 0). Env-set-but-dead = FAIL.
3. **One milestone = one commit** (`Co-Authored-By: Claude Code <noreply@anthropic.com>` trailer), NO push — the user pushes.
4. **Golden byte-pins python-first** for any new format surface (P3-M3's snapshot marker): compute the fixture in python before the Rust writer exists; a format change is a visible diff.
5. **Crash windows reuse the park harness.** Any new publication protocol (P3-M3 snapshot, P3-M7 job table, P3-M8 background merge) gets its windows covered with the existing child-kill pattern (`current_exe --exact` + env gates, KSE-15). New windows only where the protocol has new stages.
6. **Artifact discipline (P3-M0 first).** Report writes gate behind `AIKOQL_REPORT_WRITE=1`; a full local suite run must never diff committed artifacts. Nightly re-runs regenerate subsets without committing them.
7. **Security test hygiene.** No real secrets in fixtures; argon2 test vectors, not live hashes; no test binds a public interface; loopback enforcement tests bind loopback.
8. **Regressions.** Gate 5 ≤8× and the workload bounds (10/10/15% vs 09-05 baseline) hold wherever a milestone touches the engine. Suite counts recorded per milestone in this ledger's Evidence column.
9. **Honest ledger.** Anything descoped (M5c, M7b, SDK adoptions) gets a "PASS WITH ACCEPTED LIMITATIONS" closure citing evidence — the KSE/M41 pattern — never a silent drop.
10. **CI carry-over.** The existing skip list stays authoritative; new suites that are measurement-first join it or gate themselves. The dependency-DAG grep ban extends to the new deletion sweeps (clb002) and to the P4-M2 boundary pin (phy004: no `PhysicalLocation`/`SegmentId`/`BlockId` in the kernel crate).
11. **Batch wave is evidence-gated.** No batch optimization (P4-M5b–d) ships without a cell showing a real gain over the scalar path at current scale — SE2-M25 falsified naive `get_many` at 100K warm (0.73–1.13×); each sub-boundary closes with evidence or a "PASS WITH ACCEPTED LIMITATIONS" row, never a vibes green.

## Milestone gates (what flips a row to ✅)

- **P3-M0:** clb001 (report write gated), clb002 (no dangling references to deleted harnesses); full workspace suite green; zero artifact diffs.
- **P3-M1:** auth001–008 green; MCP stdio suite (90 tools) green; docker health + volume-restart green; connectors + dogfood green; no hardcoded credentials remain (grep pin: `admin/admin` absent from src). — **SHIPPED (commit 4583b68-p3m1):** auth001–008 unit green; auth_surface 2/2 spawn (remote-HTTP-without-credentials exit 2, metrics non-loopback exit 2); full crate 126/126 incl. mcp_stdio (auth007) + connector_certification; dogfood is stdio transport (untouched, auth007 pins it); grep pin: UI prefills/hints removed, remaining `admin`+`password` adjacency in src is auth001's rejection assertion (evidence of absence); docker health endpoint on the allowlist, compose change is an env pass-through (CI-covered).
- **P3-M2:** met001–006 green; met007 cell ≤1% overhead; `/metrics` series visible in a live scrape; `storage_compact` + oracle re-verify green.
- **P3-M3:** bkp001–006 green; REC-002 green; snapshot protocol crash windows covered; marker golden byte-pinned.
- **P3-M4:** cpl001–007 green; golden snapshots updated; fuzz corpus re-run clean; kernel suites untouched (regression only).
- **P3-M5:** cst001–007 green per sub-boundary; MRFC-0060 coverage table committed; existing constraint + transact suites green.
- **P3-M6:** the 1M matrix runs to completion; result artifacts committed (via report gating); gate 5 assert green; identity divergence 0.
- **P3-M7:** cb001–005 green; MRFC-0011 §11 conformance green; restart durability covered by a child-kill window.
- **P3-M8:** bgc001–005 green; the full existing compaction battery green in background mode; W8 cell recorded.
- **P3-M9:** decision doc + evidence committed; kept surfaces have green REDs, deleted surfaces leave zero dangling references (DAG green).
- **P4-M1:** ppl001–006 green (the pre-fix RED: same type + different subject/tenant/role/snapshot is deduped today — planner.rs:107 pins it); identical scans still dedup; kernel suites untouched-green.
- **P4-M2:** phy001–004 green; scalar == batch answers; relocation atomic from the reader's view; 1M oracle zero divergence; grep pin green in the DAG job.
- **P4-M3:** iv001–005 green; open refuses on impossible metadata; all existing storage suites unchanged.
- **P4-M4:** ker001–004 green; classification manifest committed; version-count and event-replay cells recorded (env-gated, reported not asserted); reference-scan parity pinned.
- **P4-M5:** rd001–005 green; every sub-boundary ships with its cell; kernel suites unchanged; no batch path lands without evidence (rule 11).
- **P4-M6:** cps001–003 green; byte-identical streamed output; park-harness crash windows covered; peak-RSS cell recorded.
- **P4-M7:** run001, idx001, vec001–002, time001, comp001, stor006 green; ingestion throughput cell recorded; overload bounded with measurable p99; kernel suites unchanged.
