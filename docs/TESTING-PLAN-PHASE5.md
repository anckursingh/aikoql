# AIKOQL Phase 5 — Testing Plan (Database 1.0)

Mirror discipline of `docs/TESTING-PLAN-PHASE3.md`: one row per milestone; status flips to ✅ only with real evidence (test names + green counts + artifacts). Requirement numbering: phase 3 ended at §73 — phase 5 sections get numbered §74+ when the architect spec is written; the roadmap's ND-xx ids are the working ids until then. Companion: `docs/IMPLEMENTATION-PLAN-PHASE5.md`.

## Rules carried from phase 3 (binding) + phase-5 amendments

1. **RED first.** Write the failing test, run it, verify it fails for the stated reason, then implement. No implementation before the RED is on the branch. Never weaken an assertion to make one green — weakening is a RED on the review.
2. **Correctness and performance separated.** Correctness pins run on every PR. Measurement cells are env-gated and reported, not asserted — except the standing asserts (gate 5 ≤8×, zero-loss, identity divergence 0, **and now gate 7 memory bounds**). Env-set-but-dead = FAIL.
3. **One milestone = one commit** (`Co-Authored-By: Claude Code <noreply@anthropic.com>` trailer), NO push — the user pushes.
4. **Golden byte-pins python-first** for any new format surface (M3's plan serialization): compute the fixture in python before the Rust writer exists; a format change is a visible diff.
5. **Crash windows reuse the park harness.** New publication/commit protocols (M10's transaction commit, M11's server lifecycle) get their windows covered with the existing child-kill pattern. New windows only where the protocol has new stages.
6. **Artifact discipline.** Report writes gate behind `AIKOQL_REPORT_WRITE=1`; a full local suite run must never diff committed artifacts.
7. **Security test hygiene.** No real secrets in fixtures; loopback-only test binds. Phase-5 extension: the encryption-parity REDs (st009 + one per M5/M6/M13 milestone) run against an encrypted database with a test passphrase.
8. **Regressions.** Gate 5 ≤8× and the workload bounds hold wherever a milestone touches the engine; **gates 6–9 apply as defined below**. Suite counts recorded per milestone in the ledger.
9. **Honest ledger.** Anything descoped (M7's non-transactional catalog entities, M8's temporal/provenance index types, M10's READ COMMITTED) gets a "PASS WITH ACCEPTED LIMITATIONS" closure citing evidence — never a silent drop.
10. **CI carry-over.** The existing skip list stays authoritative; new suites that are measurement-first join it or gate themselves. The dependency-DAG grep ban extends to the logical-layer boundary pin (qm004: no `aikoql-v2` imports in the logical plan layer).
11. **Evidence-gated work.** No batch/index optimization ships without a cell showing a real gain over the scalar path at current scale; every CBO strategy change (M9) ships with an oracle run (gate 6) or a closure row — SE2-M25 falsified naive `get_many` at 100K warm, the same discipline applies to index batch APIs (M8).

**Phase-5 addition to rule 8 — the plan-equivalence oracle (M0):** every milestone M3+ whose RED list touches the planner must include an oracle row — optimized plan vs unoptimized plan over the seeded differential corpus, divergence = 0. The oracle is delivered by M0 so later milestones consume it.

## Evidence ledger

| # | Milestone | ND | Status | Evidence (to fill) |
| --- | --- | --- | --- | --- |
| P5-M0 | Baseline certification guard | ND-00 | ✅ | gd001 pin RED→GREEN (dependency-dag requires baseline-guard.yml + the 4 protected paths + `V2ADOPT_NIGHTLY=1m`); baseline-guard.yml ships (windows-latest, path-triggered, 6h, 1m v2 leg); gd002 RED = E0432 on missing `plan_oracle` → module shipped; gd002/gd002b/gd003/gd003b 4/4; gate5-check.py on committed artifacts: W1 7.96× REDLINE, W2 5.81×, PASS; runtime 21+2+4 green; clippy -D warnings green. Pending push: the actual CI 1M run (not executable in-session) — first-PR evidence lands with it. |
| P5-M1 | Planner semantics remainder | ND-01 | ✅ | alg001 doc pin RED→GREEN (operator-algebra.md ships: 3 rewrites × preconditions/proof/pinned-by); alg002 compiler proptest 3/3 seeded — RED from injected tenant-drop regression (2 properties fail, restore green); alg002 runtime oracle proptest 256 cases divergence 0 — RED from injected merge-drop regression (caught in 9 cases); compiler 151/0 + runtime 28/0; clippy -D warnings green |
| P5-M2 | KOQL v1 remainder | ND-02 | ✅ | kq001–012 40/40 RED→GREEN (RED = 26 compile errors on the missing surface); compiler 201/0 (84 lib + golden 16 + grammar 49 + kq 40 + fuzz 4 + doc pin + proptest 3 + 4 other); full workspace green exit 0 (kernel, v2, runtime, mcp untouched-green); fmt + clippy -D warnings green |
| P5-M3 | Logical/physical model | ND-03 | ✅ | qm001–004 11/11 RED→GREEN (2026-09-13); EXPLAIN = compile_physical summary (per-op strategy, VectorIndex/FullScan visible); serialization byte-pinned (plan_golden.json exact-match); qm004 boundary pin: no storage/engine crate refs in the logical layer; compiler 212/0 incl. plan_model + kernel 257/0 + runtime green; full workspace green exit 0 (214 targets); fmt + clippy -D warnings green |
| P5-M4 | Streaming/batch execution | ND-04 | ✅ | st001–009 9/9 RED→GREEN (2026-09-14); pull-based PhysicalOperator pipeline (Scan pins koids at open, batches via kernel scan_by_type_range — same ACL/Deleted filters as materialized); cancellation → KError::Cancelled; encrypted-DB parity (st009); workspace 215 targets exit 0; fmt + clippy -D warnings green. Deferred (honest ledger): gate-7 RSS cell w/ W-suite sampler, ts-pinned reads (kernel head_object_at), W1/W2 re-run |
| P5-M5 | Aggregation + sorting | ND-05 | ✅ | ag001–008 8/8 RED→GREEN (2026-09-14); RowSet::Grouped output (first-encounter order, SQL-style nulls, mixed-types fail closed, Int+Float promotes); stable multi-key Sort over Objects+Grouped; ORDER BY accepts `count`; AS OF pin (ag007) + authorization-before-aggregation pin (ag008). Honest ledger: spill cell + gate-7 RSS ride with the W-suite sampler (strategy documented); DISTINCT deferred (no grammar clause) |
| P5-M6 | Join engine | ND-06 | ✅ | jn001–009 9/9 RED→GREEN (2026-09-14; RED = 3 E0599 on missing RowSet::Joined, committed 959ff73); LEFT JOIN grammar (`LEFT` keyword + kind in IrOp::Join); nested-loop executor, right side scanned under the caller's subject scope (cross-tenant impossible by construction — the v1 grammar cannot name a right-side tenant; jn006 pins it with kernel data); null keys never match (jn005 incl. Null==Null); oracle ≡ reference nested loop over 1 999 rows incl. a 1 000-hot-key skew (jn009); EXPLAIN exposes Strategy::NestedLoop. Honest ledger: hash join NOT implemented (nested loop is the v1 strategy; selection lands with the P5-M9 CBO seam); jn007 pins tombstones, not concurrent-write pinning (txn contract = P5-M10) |
| P5-M7 | Database catalog | ND-07→(ND-09) | ✅ | ct001–006 6/6 RED→GREEN (2026-09-14; RED = 31 E0599 on the missing kernel surface, fe53c51); catalog = journaled KOs of reserved type `aikoql:catalog` in reserved tenant `aikoql:catalog` owned by `aikoql:system` (owner-only ACL = invisibility barrier; `is_catalog_type` shared predicate); open-time `ensure()`: bootstrap at v1 / vN→vN+1 dispatch + stamp-in-place / fail-closed on corrupt or newer versions (ct003/ct006c); pre-catalog DBs re-init without touching user data (ct006a); ct004 8-thread concurrent creates serialize, ct005 idempotent + byte-stable. Derived-index maintainer skips catalog rows (scheduler guard, pinned by i04/i10). Spec migration: the bootstrap row is journal entry #1 on every fresh kernel — conformance/durability/epistemic/jobs/indexes/qa2-* journal pins now count a documented `CATALOG_PREAMBLE` (determinism law intact, t24 byte-equality holds). Canonical-only rule (first catalog-aware storage run): `write_type_index` refuses catalog types (kernel-inline path leaked one — the scheduler guard doesn't cover it) and the KSE-10 rebuild skips catalog heads; the catalog's own reads walk canonical heads via `scan_catalog_rows`, never the derived type index; storage suites count the bootstrap as per-file `CATALOG_PREAMBLE` pins (kse2/8/9/12/13/15), structural_sweep skips the catalog head. Honest ledger: non-transactional entities = schema rows; migration dispatch is an empty seam (first step ships with v2); workspace run surfaced a draw-dependent fuzz_match_parses panic (P5-M5/M6 leftover — generator's keyword list never synced with GROUP/ORDER BY + JOIN…ON); fixed at the root: the generator now asks the lexer whether a draw tokenizes as Ident (hand-maintained list deleted), 5 consecutive fuzz_parser runs green |
| P5-M8 | Unified index subsystem | ND-08→(ND-07) | ✅ | idx2-001..010 + composite 11/11 RED→GREEN (2026-09-15; kernel 6/6, scheduler 4/4, runtime 1/1); unified `trait Index` (upsert/remove/commit_batch/scan_eq/covers/verify/rebuild/len) over catalog-registered indexes — decl-shaped catalog rows (kind `index`: name/type_name/properties) with fail-closed load on corrupt rows (idx2-010); NEW PropertyIndex (hash key = Debug tuple, ANY missing key property → not indexed; upsert = replace so an update moves the entry between keys — idx2-004) + composite multi-property; vector + text engines become implementations behind VectorIndexAdapter/TextIndexAdapter — vec001/002 + text suites untouched-green, text keeps ONE Tantivy commit per batch; async maintainer applies through the adapters + fresh `kernel.property_indexes()` per drain — index commits stay OFF the commit path (idx2-003 pinned); verify = stale/missing reconciliation, rebuild = online clear+replay from canonical heads; `StreamOptions.use_indexes` opt-in → first Eq predicate over an indexed (type, property) answered from the index (idx2-008 EVENTUAL pin, off by default); spec migration: ct001's generic-entry round-trip moved from kind `index` to kind `model` (kind `index` is now M8's decl shape, not a generic payload). Honest ledger: relationship/temporal/provenance index types stay catalog-schema-only; batch APIs evidence-gated (closure row — the P4-M7 text batch path is the shipped cell preserved behind the adapter; no new property-index batch API, rule 11) |
| P5-M9 | Statistics + CBO | ND-08 | ✅ | cbo_a01–a05 5/5 + cbo001–010 (incl. cbo008b) 11/11 RED→GREEN (2026-09-15; RED 47d7ef7 = 12 kernel E0599 + 25 runtime missing-surface errors); analyze computes every roadmap statistic exactly (cbo_a01: cardinality/selectivity/fanout/densities/tenants); statistics persist across reopen + re-analyze updates in place (cbo_a02); watermark tracks the journal (fresh at capture, stale after any later event, refreshed by re-analysis — cbo_a03); corrupt rows fail closed (cbo_a04); empty type → row_count 0 (cbo_a05). CBO pins: selective Eq → PropertyIndex scan, cheaper than the rule plan AND row-for-row equal results (cbo001); low selectivity → FullScan (cbo002); fanout^depth geometric traversal cost (cbo003); density-driven ANN cost = candidates × EMBEDDING_DIM (cbo004); text = delegation walk (cbo005); hybrid modality order pinned, fusion never rewritten, leg dominance follows density (cbo006); temporal cpu = rows × (1+temporal_density) (cbo007); stale stats detected and never drive the plan, rule fallback (cbo008); a lagging index is the rejected piece with fresh stats (cbo008b); EXPLAIN COST lines + statistics footer none/fresh/stale (cbo009); gate-6 `run_costed` oracle 6 scenarios 0 divergences (cbo010). RED corrections recorded: update requests restate properties (kernel update replaces the property map); fanout from distinct targets (edge-set index collapses duplicate triples); `AS_OF` token. Workspace green; fmt + clippy -D warnings green |
| P5-M10 | Transaction contract | ND-10 | ✅ | tx001–007 (incl. tx006a/tx006b child-kill commit window, tx007 across reopen) 9/9 RED→GREEN (2026-09-15; RED a8246b9 = 16 E0599 on the missing surface); begin/stage/commit/rollback + SNAPSHOT reads; deterministic VersionConflict from the stage-time OCC pin; idempotent retry via the same-batch outcome row; metrics (begun/committed/rolled_back/conflicts/deduped_retries); SNAPSHOT-only honest-ledger row |
| P5-M11 | Standalone server | ND-11 | ✅ | sv000–011 12/12 RED→GREEN (2026-09-15; RED 27a1f89 = pre-impl suite — see gate row); KOQL protocol adapter (`koql/query` read-only + `koql/execute` + `shutdown`) over the framed TCP transport, fail-closed token gate; graceful shutdown (ack → EOF → drain cancels in-flight tokens + actively closes remaining sockets → exit 0); request timeout (-32002, worker + CancellationToken); connection cap (-32000 reject at the margin); 1 MiB frame cap; the P5-M10-deferred txn tools over a connection-scoped handle registry; crash windows AIKOQL_QUERY_PARK (one-shot) + _PARK_MARKER/_EXIT_MARKER. Honest ledger: disconnect-cancellation latency = request timeout (no read-wakeup on drop); check-then-act cap race; non-parking queries finish after cancellation (response discarded); mvp_rec_002 EOF flake once (documented respawn-lock class; passes isolated + full rerun) |
| P5-M12 | CLI + SDK | ND-12 | ✅ | cl01a–g + cl03a–g 13/13 + cl02 4/4 RED→GREEN (2026-09-15; RED 1ea954d); error-code table `docs/error-codes.md` pinned two-directionally (cl01a needles, cl01b source→doc completeness); producer pins for previously-untested codes (-32601, ack-path -32603, -32004 startup refusal, envelope VALIDATION_ERROR/NOT_FOUND/COMPILE_ERROR/INTERNAL); `docs/version-compatibility.md` + MIN_SERVER_VERSION self-pinning + initialize fail-fast VERSION_MISMATCH (fake old server + real binary pins); five verbs dogfooded through the repo-built binary (status/query/explain/index/schema = thin wrappers over the tool implementations); usage errors exit 2, runtime errors exit 1 with the kernel tag on stderr. RED corrections while going green (recorded): cl01b wire scan was a 5-char window (fixed → 6), envelope scan scoped to as_str() (suggestion() also matches), kom.rs path missing `crates/`, trailing-quote break guard; txn_begin returns its payload directly (no ok wrapper) |
| P5-M13 | Hybrid certification | ND-13 | ✅ | h1–h6; hand-computed oracle per modality; EXPLAIN shows every modality |
| P5-M14 | Database certification | ND-14 | ✅ | cert000–003 5/5 RED→GREEN (2026-09-15/16; RED 3fbbdf0 = E0432 on the missing `aikoql-certification` surface); `crates/certification` = the ND-14 runner: five DB-* suites (db-oltp/db-graph/db-vector/db-knowledge/db-agent) on a seeded AikoqlStorageEngineV2 kernel, every workload cell pins the acceptance schema (name/n/p50/p95/p99/throughput/RSS/disk/cold/warm/correct) + correctness_parity; cert001b pins same-seed determinism; cert002 pins detection power (CERT_INJECT=1 corrupts the seed → fail-fast, no artifact); cert003 pins DB-AGENT evidence coverage + provenance completeness. Artifacts in `docs/certification/` stamped with run-date + commit hash (regenerated per commit via `cargo run -p aikoql-certification --example generate`). Honest ledger: concurrency = threads over ONE kernel (the mcp shape — a second Kernel over one store contends on the store-global journal head); recovery uses per-sample clocks (KOIDs encode the HLC — same clock+sequence regenerates koids → OCC VersionConflict); BFS pre-seeds visited with the start koids so edges back into the seed set are never emitted (rel_filter oracle uses a bird→e5 derived_from edge); "transaction" = the kernel's single-op atomic unit (crash injection is SE2-M36's certified surface — certification pins restart durability); CERT_INJECT is process-wide → INJECT_LOCK serializes the injection test; competitor comparisons = published reports, not CI gates |
| P5-M15 | CBO on the default query path | post-close | ✅ cbo-default 4/4 (7c0d7e1); runtime green; kernel+mcp+storage-v2 1027/0; cert 5/5 re-stamped; competitor re-stamp dc42b16: filter 19.9 ms — the ~2.5 ms expectation FALSIFIED (production gap: no declaration surface, no maintainer, no stats — guards correctly fall back; re-scheduled as P5-M17b) | cbo-default suite: default-path execute() on an indexed+fresh kernel = PropertyIndex strategy, row-for-row identical to FullScan; stale stats / lagging index → FullScan; no declared indexes → byte-identical to the pre-M15 path; gate-6 run_costed through the default path divergence 0; competitor structured_filter cell re-measured and republished (one harness run after M16 covers both cells) |
| P5-M16 | Vector recall without corpus materialization | post-close | ✅ vs-scan 3/3 (b257844); kernel 37 suites green; aikoql-vector 7/0 untouched-green; storage-v2 64/0 (bkp002 timing pin flaked once, passes isolated + full rerun — recorded); runtime 10/0; mcp 8/0; cert 3/0 + artifacts re-stamped (1e07731, machine-drift note: back-to-back pre-M16 control measured the same drift); competitor re-stamp dc42b16: vector cell 12.0 → 5.0 ms warm p50 (2.4×, parity with Qdrant 5.3; control cells flat — signal, not drift) | vs-scan suite: ranking parity vs brute-force reference (ACL-denied + no-embedding rows included); materialization-count probe (k=10 over 1 000 → ≤ top-k + margin reads, the old path reads 1 000 — fails pre-impl); index_lag_ms unchanged; vec001/002 untouched-green; competitor vector cell re-measured (the ~1 ms prediction was optimistic — the harness includes Python SDK + response materialization; actual 5.0 ms, 2.4× over the pre-M16 stamp; the cert db-vector suite carries the tighter measure) |
| P5-M17 | Benchmark scale-out + honest cells | post-close | ✅ 2026-09-16 (stamp 3152c5e) | harness gains 100k/1M scale runs (filter/vector/graph/point_read — filter lands pre-17b, the scan story, re-stamped after), a multi-op transaction cell (batch 10/100 via P5-M10, one commit) vs PG multi-statement txn, and one MCP-mode column (localhost aikoql-mcp); oracles stay the correctness pins; docs/certification/competitors/scale/ republished — full run exit 0, all 22 oracles correct. The MCP column caught a real defect: structured_filter 725 ms warm p50 over the wire at N=1 000 vs 9.11 ms embedded (79×) → bisected to `write_frame` Display-serialization (one write() syscall per JSON token, 33 008 per 127 KB frame); RED = the bounds-writes pin (504af08, fails 33008 > 2 pre-fix), fix 3152c5e → 18.1 ms (40×), all column cells lifted (shared write path). M18 gate resolved: vector 1 092 ms @100k → 17 002 ms @1M (15.6×/10×) → SHIP, with the candidate-driven-ranking scope note (vmap hole, ann001 RED). Full tables + caveats in REPORT.md |
| P5-M17b | Power the property index in production | post-close | ✅ Shipped 2026-09-15 (6873bcc) | SDK `create_index` + MCP `index_create` (same call shape), IndexMaintainer started unconditionally in both hosts (MCP serve + embedded SDK kernels), stats computed behind the declaration (wait-caught-up pattern); guard chain byte-identical to M9/M15 (cbo_default_001/002/005 + idx2_010 + cbo_a06 — incl. the two O(store)-per-query fixes the acceptance exposed: idx.verify's applied-seq O(1) short-circuit and statistics()'s parsed-stats cache, both still fail-closed on staleness). Harness re-measure (N=1000): structured_filter 26.2 → 9.11 ms warm p50 (PG indexed 2.10); point_read 0.010 vs PG 1.85. The ~2.5 ms aspiration was NOT met — the residue decomposes honestly (per-row readable-object point reads, PyO3 row marshaling, executor filter/clone passes — all pinned by the M15 byte-identical contract or the SDK shape); full decomposition in REPORT.md. Scale filter re-stamp lands with M17 |
| P5-M18 | ANN vector index | post-close | ✅ ann001–003 3/3 + idx2-011 (cc34bc8, ff466df, bee8751) | HNSW behind trait Index + coordinator seam in BOTH hosts; candidate-driven vector leg (ann001); Weak-coordinator lock-leak fix + Drop-joins-thread (python test_close_releases_store_lock_for_immediate_reopen); ann003 IR delegation (un-narrowed Scan only, recall-guard fallback); replay batches 64 events/commit (idx2-011). 100k/1M evidence re-stamp PENDING (the 2026-09-16 harness was killed after 14h — full-replay-per-connect; M19/M22 close it). PR6-review-open rows (capacity evidence, checkpoint manifest, physical accounting, model-identity invariant) → P5-M23 |
| P5-M19 | Secondary-index convergence | PR6 review | ✅ 1bd672d RED → 28104c8 feat | idx3-001 failed batch retried in place, never dropped (status CaughtUp only after every committed event is applied); idx3-002 barrier-pinned startup commit lands in the index (subscribe-first + drain ≤ water); idx3-003 re-type membership removed, zero re-adds; idx3-004 stage-2 failure retains the removes — retry flushes with no caller re-queue; idx3-005 consistency classes — CandidateOnly refused by scan_assist (may_serve_exact). kernel/scheduler/runtime suites green |
| P5-M20 | Transaction & job lifecycle | PR6 review | ✅ edd21d7 RED → c482dc0 feat | tx008 staged-write visibility pinned to the documented contract; tx009 same txn_id + different body fails closed (InvalidObject); tx010 same body → recorded retry; job001 CAS admission (16-thread barrier, max=1, slowed engine → exactly one); job002 audit-failure compensation (no orphan Running row / by_hash entry / slot — FlakyEngine trips the repo's ke/+meta/journal batch, ke/: the arm is consumed only by an audit batch); job003 restart dedup per state (Completed dedupes across reopen, Failed retries fresh); job004 tenant isolation of Class-B workers (the kernel-reason/admin hard-code removed — the caller's subject is part of the job identity and the worker's scan). tx006a/b + tx007 + sv011 re-applies updated to same-body |
| P5-M21 | Query snapshot & index assist | PR6 review | ✅ 563e345 RED → 768fffd feat | st010 write between batches → open() snapshot (fails today, mixed-time); st011 plan carries snapshot/freshness assumptions; cbo011 write between optimize and execute → pinned-head serve or scan fallback; cbo012 non-adjacent Filter never index-assisted. Gate 11 |
| P5-M22 | Index DDL lifecycle + checkpoint/resume | PR6 review | ✅ idx4-001..004 + 001b/002b (d063a50 RED → f1dea6c) | idx4-001 DDL crash converges to one state; idx4-002 checkpoint at H → restart replays only events after H (commit-count spy, idx2-011 pattern); idx4-003 manifest generation mismatch fails closed; idx4-004 DROPPING index never CBO-chosen |
| P5-M23 | Production ANN hardening + evidence | PR6 review | ✅ 02c5526 RED → acc0c43 feat | ann004 64 into capacity-8: k=10 answers 10, no drop, physical/live 64, self-recall first, health reports capacity+dim; ann005 repeated (koid, model) upsert → physical stable (the re-upsert's vector answers); ann006 AnnSearch cpu = rows × the LIVE ANN dim (2-d fixture; 768 only when no maintainer); evidence cell AIKOQL_ANN_EVIDENCE=10_001 PASS — no panic/drop, physical==10_001, recall@10 ≥ 9/10 vs brute-force oracle (24.9s). CBO → CBO v1 / cost-guided physicalization in docs; P1-18 one-embedding-per-KO invariant documented (KOID-level tombstones identity-correct). 100k/1M harness re-run with the ANN live: KILLED by decision 2026-09-18 ~12h into the 1M section (per-connect create_index re-declaration at 1M still prohibitive — 12 fresh opens, each rebuilding over the full corpus; the 100k section completed first: ingest 201.9s, RSS 231.5 MB). No result.json. REPORT.md re-stamp deferred to the CI workflow run — laptop = --quick smoke only, full harness moves to the workflow |
| P5-M24 | Product defaults + execution-path scale | PR6 review | ✅ f562a17 RED → 27442e2 feat | cl04 fresh shell writes+reopens v2 (P1-21: default = ./aikoql-v2 dir, redb explicit); sv012 CAS connection cap (P1-19, P0-04 fetch_update admission, served ≤ 2 pin); idx4-005 cells: 1k 0.003s/0.000s, 100k 112.960s/0.006s, 1M 14085.988s/0.190s (upsert hi-card / scan_eq lo-card — the O(buckets) upsert sweep goes superlinear past 100k: rule-11 reverse-map evidence); D2 reader matrix read_1t..read_8t through the one kernel (db-oltp, cert001 schema); D3 memory governance (per-workload RSS + governance workload pinning full materialization/LIMIT + server-contract §Query resource governance); D4 client-server column — deferred to the CI workflow re-run (laptop full run killed by decision 2026-09-18; local = --quick smoke only) |
| P5-M25 | Block-cache hit path O(1) | post-close | ✅ | cache_hit_does_not_scan (scan-steps pin, fails 64 > 0 pre-fix — f189184) + eviction_targets_least_recently_hit (LRU guard) — RED→GREEN in f87cda1; storage-v2 suite green. Hit = hash + stamp; min-gen eviction scan lives on the miss path |
| P5-M26 | Per-connect rebuild no-op + property checkpoint | post-close | ✅ | freshness guard (applied==head && Ready skips the O(heads) reseed); maintainer checkpoint carries the AKPI property-index files (water-first, restore-before-tail); competitor-scale job wired into benchmark-nightly.yml. Traps: SchedulerJob::checkpoint needs the kernel; mcp main.rs is Arc<Kernel> |
| P5-M27 | Crash matrix + PR6 plan closed | post-close | ✅ | all seven PR6 remediation items closed (b2aa690 RED → f083dea → 439d8fc), crash matrix green — M19–M25 closed. 2026-09-19 CI 35381873058: mvp_rec_002 flake root-caused (9f23b7a RED → 2abc9fa, per-child start_with) + SDK tantivy panic (4f691ad RED → 3b59656, TopDocs limit-0 guard) fixed locally, user pushes |
| P5-M28 | Reader-based WAL replay (P0-01+P1-01) | PR6 perf review | ✅ | rpl001 WAL-size-independent recovery allocs (10 MB vs 100 MB, counting allocator) — GREEN 21.0 vs ≤25 MB armed (RED 115.4 MB); rpl002 byte-equivalence vs decoder corpus — GREEN incl. 64 KiB chunk-straddle case; rpl003 torn-tail truncation — GREEN (on-disk WAL == exact prefix, reopen clean); rpl004 damage-then-valid fails closed — GREEN (Corrupt, on-disk WAL untouched); rpl005 seq monotonicity — GREEN (equal + decreasing Corrupt, increasing applies in order) |
| P5-M29 | Sorted-input publish (P0-02) | PR6 perf review | ✅ 10c9d7c RED → 756c938 feat | fls001 100k memtable-ordered publish peak 933,401 vs 1,800,000 budget (RED 7,200,000 — the whole-buffer sort scratch is gone); fls002 sorted ≡ sorting publish byte/checksum/anchor-identical over a block-spanning version corpus (anchors compared as sets — Vec order is HashMap-iter order, the flush sorts by rid itself); fls003 dup-(key,seq) → Invalid, no file; fls004 decode = key asc + seq desc within key, rids preserved; fls005 per-rid max-seq anchors exact. 100k/1M wall cells env-gated (honest ledger) |
| P5-M30 | O(log n) cache eviction (P0-04) | PR6 perf review | ✅ f3ccb8e RED → e45bbe0 feat | cch001 one eviction moves scan_steps by 0 (RED 64); cch002 flat at 16/512/4096 (RED n per victim); cch003/cch004 untouched-green; cch005 churn keeps heap ≤ 2× map |
| P5-M31 | Allocation-free memtable reads (P0-03) | PR6 perf review | ✅ bdbe16b RED → 0e74083 feat | mtr001 armed peak 0 over 50k reads (RED 7 B/key copy); mtr002 warm p50 1800→900ns, p99 3600→1500ns (cell, not gated); mtr003 prefix parity + mtr004 any-order/replace/version-heavy guards green; flush order = key asc + seq asc by construction (M29 goldens re-run green) |
| P5-M32 | Group-commit alloc cleanup (P1-02+03) | PR6 perf review | ✅ d676f89 RED → f7d81cf feat | grp001/grp001b order-independent exact-fit/carry-over pins (3× 2-op batches vs cap 3 → exactly 3 fsyncs; 3× 200-byte vs cap 250 → 3 fsyncs; 7/7 guards — green on the old fold, reproduced exactly by the running counters); grp002 counting-allocator pin: 3244 allocs over 200 groups with the hoisted seqs buffer vs 3443 with the per-group Vec (budget 3350 between the two shapes; both counts zero-spread over 3 runs); fold → O(g) running counters |
| P5-M33 | Snapshot lock redesign (P1-04) | PR6 perf review | ✅ 3c2cba9 RED → 604606b feat | snp001 put completes while parked mid-copy (RED: blocked by the read lock); snp002 checkpoint lands during the parked copy, child-kill leaves the dir unmarked + ≤ g0 only (RED: checkpoint blocked); snp003 prune skips pinned IDENTITY-g0, removes unpinned IDENTITY-g1 (RED: flush+checkpoint blocked); snp000 protocol-doc grep pin; PR6-007 matrix 10/10 with rows 1–4 re-documented lands-concurrently (sfm004 flips to pinned-segments-survive) |
| P5-M34 | Snapshot double-read measurement (P1-05) | PR6 perf review | ✅ 416400a RED → feat | snp004 GREEN: capture 3 ms / copy 2743 / verify 2464 / marker 3, 67.1 MiB copied, read 134.2 MiB (= 2×, the double-read as data), RSS < 48 MiB bound, restore byte-exact — the ~64 MiB generation |
| P5-M35 | Control-plane lock + L0-scan profile (P1-06+07) | PR6 perf review | ✅ RED → feat | prof001 GREEN: DbStats.control counts stats()/resolve guard waits (pooled per family, wait-only ns); prof002 GREEN (gated): scan 345 ns/write = 0.7% of the ~49 µs write path — immaterial, the per-write scan stays, publication-time counters NOT shipped (the evidence said no) |
| P5-M36 | Compaction scale profile (P1-08+09) | PR6 perf review | ✅ | cp001 RED 60add89 → feat: CompactStats.rids_seen first-class (the seen-set denominator); laptop cells (debug, 1M × r{1k,100k}): wall 28.3/26.3 s, RSS +164.6/+169.2 MiB, allocs 2.03N (the 4N PERF-3 budget holds at scale), rids_seen == r; rid-side ≈ 45 B/rid → 2.7% of the bulk peak at r=100k → HashSet KEPT (rule 11: no custom structure without a gain cell); 10M points env-gated (AIKOQL_V2_CP_CELLS_FULL=1) for CI |
| P5-M37 | Comparative latency gate + units (P1-10+11+12) | PR6 perf review | ✅ bench001/002 (0c41091 RED → ff2678a feat); bench003 + 1M republish pending | contract module (common::contract): rows emit p50_ns/p95_ns/p99_ns (the old *_us carried ns — bench002 pins the mismatch rejection), required sections/env/dataset keys named on rejection (bench001), verdict as a string never null; both kse_m7 harnesses renamed, gate5-check.py + perf-smoke-check.py readers follow; bench003 companion benches + the 1M republish ride the next session (full runs on the CI workflow) |
| P5-M38 | Flush lock-scope split (R4-P0-01) | PR6 R4 review | ✅ 69fe49c RED → e81bb1b feat | fsc001–004 4/4: a put completes while the flush is parked in its UNLOCKED segment I/O (AIKOQL_V2_FLUSH_IO_PARK=in_io); the four counters (flush_total_ns/flush_state_lock_hold_ns/flush_io_ns/flush_publish_ns) populate on a real flush with hold (A+C) + io (B) ≤ total — the disjoint-window structural invariant, not a wall threshold; a compaction completing during the parked I/O survives the publication (phase C builds the manifest from state read fresh under the C lock — never stale); a second flush cannot publish while the first is parked (the flush_pipe serializes all three call sites). Mid-milestone correction (pr6_004 caught it): a write can append+apply between A and C, so truncate-to-zero destroyed an ACKED write across close/reopen — phase C now keeps exactly the uncovered WAL tail (save/reset/re-append; honest-ledger row). pr6_004 deadlock matrix green (10/10 re-runs), full storage-v2 suite green, workspace regression green |
| P5-M39 | Compaction merge outside the lock (R4-P0-02) | PR6 R4 review | ✅ 9481962 RED → eb30aa0 feat | csc001–003 3/3: a put completes while the merge is parked in its UNLOCKED segment I/O (AIKOQL_V2_COMPACT_PARK=in_io); the four counters (compact_total_ns/compact_state_lock_hold_ns/compact_io_ns/compact_publish_ns) populate on a real merge with hold (A+C) + io (B) ≤ total — the disjoint-window structural invariant, not a wall threshold; a flush completing during the parked merge marks it stale — discard, publish nothing, CURRENT stays at the flush's generation, no staging residue. Deviation from the disposition (forced by the chunk geometry): no A-time id reservation — the chunk count is unknowable pre-merge — so the merge runs into a `.compact-staging-{pid}-{nonce}` child of the data dir, C allocates real ids under the lock and renames staged→real, remapping chunk/archive/relocation ids; the SEGMENT crash windows' park markers moved into staging with the published file's parent (compact_crash / ci001-002 / merge_chunk crash-after-segment re-pinned to the staged namespace — same window semantics, the reopen sweeps the residue — honest-ledger row). Full storage-v2 suite green, workspace regression green (the cp001-at-1M cells ride the CI workflow) |
| P5-M40 | O(1) L0/L1 backlog accounting (R4-P1-01) | PR6 R4 review | ✅ 95f4506 RED → feat | lba001/002 2/2: zero scan_l0 calls per write at 10/100 segments (laptop) and 1,000/10,000 (env-gated AIKOQL_V2_LBA_CELLS_FULL=1) — the review's sweep pin structurally (zero scans at every scale, never a wall cell); counters ≡ scan_l0 parity after every flush/compaction/reopen (debug_scan_l0 the validator — the M35 counters now count only validator runs); met003 gauges byte-identical (the M35 free ride preserved). Authoritative l0_count/l0_bytes/l1_bytes in State: open = one scan_l0 pass, flush C = O(flushed) fold (L0 only), compact C = one recompute per merge. prof002 flipped to the M40 reality (both regimes record 0 scans / 0 ns per 100k writes — the M35 cell's own counter 100k → 0). Reverses M35's keep — recorded, not silent (honest-ledger row) |
| P5-M41 | Sorted compaction publish (R4-P1-02) | PR6 R4 review | ✅ f685711 RED → f5c3bf8 feat | `publish_with_anchors_sorted_staged` (the M29 sorted path + the SE2-M36 park stage) with a debug_assert pinning the heap's key-asc/seq-desc contract (it caught a fixture bug on its first GREEN run — the corpus wasn't key-ascending); sst001 byte/checksum/anchor-identical over a block-spanning corpus (the sorting writer fed a scrambled permutation — its sort still earns its keep); sst002 duplicate-(key,seq) → Invalid, no file; live + archive publishes feed heap order straight through — compaction never sorts; M29 goldens + PR6-007 crash matrix green; cp001 re-run: walls 20.5/21.5 s vs the M36 28.3/26.3 s (cross-day caveat), RSS +160.8/+165.3 MiB vs +164.6/+169.2 MiB (−2.3%), allocs 2.03N identical; three one-off suite incidents green standalone + on the arbiter re-run (honest-ledger rows) |
| P5-M42 | get_many O(1) resolution tracking (R4-P1-03+P2-02) | PR6 R4 review | ⬜ | Vec<bool> positional resolution (kills the O(B²) retain at db.rs:1619, verified) + Vec<Option<(u64,u64)>> bloom hashes (db.rs:1572, verified); the review's batch sweep 128/512/1K/4K/16K × {one segment, spread} with the allocation pin (allocs not worse than O(B)); correctness parity vs the current path. (SE2-M25's batch-vs-loop falsification was about the API gain — both stand) |
| P5-M43 | Allocation-free scan equal-key drain (R4-P1-04) | PR6 R4 review | ⬜ | resolve the newest-layer winner inline while draining — winner + consumed stream indices only, never a candidate clone (db.rs:1794-1807, verified); zero-alloc drain pin (counting allocator, version-heavy scan); winner parity vs the current path; the review's scan cells 10K/100K/1M × p50/p95/p99/allocs/bytes/rows-per-sec (env-gated; 10K/100K laptop, 1M CI); W5 re-stamped. Dedicated scan-performance milestone |
| P5-M44 | Restart-index preparse (R4-P1-05) | PR6 R4 review | ⬜ | parse once at open (or OnceLock) into a compact RestartIndex (segment.rs:1439 rebuilds per lookup, verified); measure metadata-bytes/segment, open time, point-read CPU + allocs FIRST, select the representation on the tradeoff (the review's own memory gate — no blind per-segment bloat); reparse pin (a lookup no longer rebuilds the restart vector); metadata budget pin; byte-identical lookups (kse golden corpus) |
| P5-M45 | Cache concurrency benchmark (R4-P1-06) | PR6 R4 review | ⬜ | the review's matrix — 1/2/4/8/16/32 threads × {same hot block, random blocks, mixed segment readers} × {throughput, p50/p95/p99, cache mutex wait}; shard into 8–16 locked shards ONLY if the cells show contention (the review's own gate — no sharding before measurement) |
| P5-M46 | Two-tier replica dedup (R4-P2-01) | PR6 R4 review | ⬜ | benchmark k = 2/8/32/128/512/4096 FIRST (compaction.rs:184/219 Vec + contains, verified); the two-tier (Vec below the crossover, HashSet above) only where the cells show a crossover worth the branch (rule 11); correctness parity — rids_seen unchanged (the M36 pin re-run) |
| P5-M47 | Benchmark evidence + CI integrity (R4-P2-04/05/06/07) | PR6 R4 review | ⬜ | artifact-freshness CI check — environment.git_sha == tested HEAD, fail on mismatch (the committed 75391b82 artifact is the live RED — verified stale vs head c19cee5); schema validation in gate5-check.py + perf-smoke-check.py — deterministic named errors ("missing field: p50_ns"), never a raw KeyError (fixture pins); the fast-PR vs protected-1M split is already the PR6-008 ci.yml shape — the remaining item is re-verifying the budget on the next CI round; the M37-remainder 1M republish with the *_ns fields + verdict strings lands here (CI workflow, full runs) |

## Milestone gates (what flips a row to ✅)

- **P5-M0:** gd001–003 green; the guard job runs on the four protected paths; corpus reproducible from clean checkout.
- **P5-M1:** alg001–002 green; proptest suite in CI; algebra doc committed and grep-pinned.
- **P5-M2:** kq001–012 green (incl. kq009 fail-closed on both compile paths, kq010 Int literals, kq011 serde round-trip); existing compiler suites extended green (golden 16/16, grammar_coverage 49/49, fuzz 4/4); MCP `aikoql` tool untouched-green.
- **P5-M3:** ✅ 2026-09-13 — qm001–004 11/11; EXPLAIN shows the per-op strategy; serialization byte-pinned (plan_golden.json, exact-match).
- **P5-M4:** ✅ 2026-09-14 — st001–009 9/9; encrypted-DB parity green (st009); gate-7 RSS cell + gate-5 1M re-run deferred (honest ledger: sampler harness not yet wired to this suite).
- **P5-M5:** ✅ 2026-09-14 — ag001–008 8/8; AS OF + authorization pins green; spill strategy documented, env-gated cell deferred (W-suite sampler, honest ledger — no production claim).
- **P5-M6:** ✅ 2026-09-14 — jn001–009 9/9; cross-tenant JOIN impossible by construction (single subject scope, jn006 pin); oracle ≡ reference nested loop green (jn009); EXPLAIN shows NestedLoop.
- **P5-M7:** ct001–006 green; migration deterministic; non-transactional entities on the honest ledger.
- **P5-M8:** ✅ 2026-09-15 — idx2-001..010 + composite 11/11; vec001/002 + text suites untouched-green; index commits off the critical path (idx2-003 async-maintainer pin); batch APIs closed with a closure row (honest ledger).
- **P5-M9:** ✅ 2026-09-15 — cbo_a01–a05 5/5 + cbo001–010 (incl. cbo008b) 11/11; gate-6 run_costed oracle 0 divergences over 6 scenarios (index-assisted, full scan, no filter, ORDER BY LIMIT, GROUP BY, AS_OF); statistics persisted + stale detection + corrupt-row fail-closed pinned; EXPLAIN COST lines; workspace green; fmt + clippy -D warnings green.
- **P5-M10:** ✅ 2026-09-15 — tx001–007 green (incl. tx006a/tx006b commit-crash windows via the txn_crasher example, tx007 across reopen); SNAPSHOT-only documented (docs/transaction-contract.md, grep-pinned by tx000); no async suspension inside critical locks (tx000 grep pin over kernel.rs + txn.rs); kernel suite + workspace regression green; fmt + clippy -D warnings green.
- **P5-M11:** ✅ 2026-09-15 — sv000–011 12/12 (RED 27a1f89 committed pre-impl: contract-doc needles, clean-shutdown zero-loss reopen, restart recovery, concurrent-session isolation + connection limit, fail-closed auth + token-less exit 2, tenant-scoped KOQL, malformed-frame -32700 + oversized-line drop + server-stays-up, disconnect-cancellation + server-keeps-serving, slow-client backpressure, drain-cancels-mid-query with acked-write survival, child-kill zero-loss, txn tools round-trip incl. idempotent retry + rollback); KOQL adapter round-trips through the repo-built binary; suite runs in 34s (drain active-close, not the 30s backstop). RED corrections while going green (recorded): the suite's reopen helper expected the RedbEngine but the server's default fresh backend is the aikoql-v2 DIRECTORY — helpers now use `AikoqlStorageEngineV2::open`; bare `MATCH Node` is invalid (the dialect requires RETURN); sv006's oversized-line write tolerates ConnectionReset (Windows RST on drop); the park hook is one-shot (sv007's post-cancellation client gets service). Workspace: mcp 143/143, kernel + runtime + v2 green; fmt + clippy -D warnings green.
- **P5-M12:** ✅ 2026-09-15 — cl01a–g + cl03a–g 13/13 + cl02 4/4; error-code table committed and pinned in both directions; SDK fails fast on VERSION_MISMATCH against an older server; each new CLI verb dogfooded through the repo-built binary; mcp 156/0, kernel 576/0, runtime 66/0, v2 295/0, Python SDK 22/22; fmt + clippy -D warnings green.
- **P5-M13:** h1–h6 correctness pins green; env-gated latency cells recorded; ACL sub-queries pinned. ✅ 2026-09-15 — hybrid_h_suite 7/7; two real gaps found by the RED and closed (Fuse fused text-with-text; Traverse rejected Scored input); explain_cost shows every modality; machine-readable report verified (target/hybrid_bench/result.json, rows match every oracle).
- **P5-M14:** ✅ 2026-09-15/16 — cert000–003 5/5 (RED 3fbbdf0); all five DB-* suites reproducible from clean checkout (same seed → same workload shape, cert001b); injected regression fails the run and writes no artifact (cert002); DB-AGENT evidence coverage ∈ [0,1] + provenance closed + deterministic (cert003); five artifacts committed in `docs/certification/` with run-date + commit hash; kernel/runtime/mcp/v2 suites green, Python SDK 22/22; fmt + clippy -D warnings green.
- **P5-M19:** ✅ 2026-09-17 — idx3-001..005 green; the review's A1 list covered; a failed batch is retained and retried — status never reports CaughtUp across unapplied events; `stamp == head` certifies only applied events.
- **P5-M20:** ✅ 2026-09-17 — tx008–010 + job001–004 green; retry identity = txn_id + body fingerprint, fail-closed on mismatch; docs/implementation/certification assert the same transaction semantics (the db-oltp multi_txn cell commits five staged creates in one batch); no orphan Running rows or leaked admission slots.
- **P5-M21:** ✅ 2026-09-17 — st010/011 + cbo011/012 green; streaming ≡ materialized for the same snapshot; every index-assisted plan states its snapshot; EVENTUAL use is explicit.
- **P5-M22:** ✅ 2026-09-17 — idx4-001..004 + 001b/002b green; startup replay cost ∝ events after the checkpoint (the spy sees only the N post-checkpoint commits apply); water > head fails closed; DDL and the live registry never diverge durably.
- **P5-M23:** ✅ 2026-09-17 — ann004–006 green + the 10_001 evidence cell PASS; the 100k/1M re-run was attempted 2026-09-18 and killed by decision ~12h into the 1M section (per-connect index re-declaration at 1M still prohibitive; the 100k section completed: ingest 201.9s, RSS 231.5 MB) — full harness moves to the CI workflow, REPORT.md re-stamp lands with that run.
- **P5-M24:** cl04/sv012 green; the D-wave cells recorded; competitor REPORT re-stamped in client-server mode.
- **P5-M28 (gate 12 applies):** rpl001–005 green; open() never materializes the WAL; existing recovery/wal suites green untouched; peak memory O(max frame + memtable delta).
- **P5-M29 (gate 12):** fls001–005 green; flush never calls entries.sort_by; golden corpus byte-exact; flush alloc + wall cells recorded.
- **P5-M30 (gate 12):** cch001–005 green; touch O(1) preserved, evict amortized O(log n), no unsafe; storage-v2 suite green.
- **P5-M31 (gate 12):** mtr001–004 green; zero allocs per point read; memtable/db/v2 suites green; flush output byte-identical (M29 goldens re-run).
- **P5-M32 (gate 12):** grp001/002 green; group commit allocates once per group; commit-path suites green.
- **P5-M33 (gate 12):** snp001–003 + snp000 green; writer latency decoupled from snapshot duration (the only writer-blocking span is the capture hold — a small WAL copy, bounded because every flush truncates); failure-window tests green (PR6-007 matrix 10/10); bkp suites green (storage-v2 real cargo exit 0).
- **P5-M34 (gate 12):** snp004 GREEN — the cells are recorded and reported (laptop cells: capture 3 ms, copy 2743 ms, verify 2464 ms, read = 2× copied, RSS under bound); protocol UNCHANGED — the verify re-read is the mutation-detection proof, no single-pass replacement can prove it (the RED states this).
- **P5-M35 (gate 12):** prof001/prof002 green; the decision cell recorded — scan 345 ns/write, 0.7% of the write path, immaterial; the lock-wait instrumentation is live (DbStats.control) and the publication-time l0/l1 redesign is NOT shipped — the evidence said no (the gate's own rule).
- **P5-M36 (gate 12):** 1M profile cells recorded on the laptop, 10M points env-gated for CI; no custom structure shipped — the gain cell said 2.7% (rule 11).
- **P5-M37 (gate 12):** contract-schema harness green (bench001/002, 0c41091 → ff2678a); comparative republish with baseline/candidate commits + ratios pending the 1M CI runs; cert suites untouched-green.
- **P5-M38 (gate 12):** ✅ 2026-09-22 — the state lock does not cover segment file construction — hold (A+C) + io (B) ≤ total structurally (fsc002, instrumented not wall-asserted); parked-flush isolation green (fsc001); stale-publication pin green (fsc003); flush-pipe pin green (fsc004); M29 goldens byte-identical (the sorted-input publish path unchanged); PR6-007 crash matrix green (full-suite crash tests).
- **P5-M39 (gate 12):** ✅ 2026-09-22 — the state lock does not cover merge construction — hold (A+C) + io (B) ≤ total structurally (csc002, instrumented not wall-asserted); parked-merge isolation green (csc001); stale-publication pin green (csc003 — never publish from a stale snapshot); the SEGMENT crash windows re-pinned to the staged namespace (same window semantics, the reopen sweeps the residue), PR6-007 matrix green via the crash suites; cp001 cells at 1M ride the CI workflow (honest ledger — no bench harness in the workspace).
- **P5-M40 (gate 12):** ✅ 2026-09-22 — the sweep cells recorded and flat across 10..10,000 segments (zero scans per write at every scale — the structural pin, never a wall assert); met003 gauges byte-identical; counters ≡ scan_l0 after every flush/compact/reopen (lba002). The M35 keep reversal is on the honest ledger.
- **P5-M41 (gate 12):** ✅ 2026-09-23 — compaction never calls entries.sort_by (sst001 byte/checksum/anchor-identical; the order-pin debug_assert caught a fixture bug on the first GREEN run); duplicate-(key,seq) validation unchanged (sst002); the SE2-M36 park green; cp001 re-run walls 20.5/21.5 s vs the M36 28.3/26.3 s (cross-day caveat), RSS −2.3% ≈ unchanged, allocs 2.03N identical — CPU down, peak memory unchanged (the review's own list).
- **P5-M42 (gate 12):** the batch sweep cells recorded; allocs O(B); correctness parity green.
- **P5-M43 (gate 12):** the zero-alloc equal-key drain pin green; scan cells recorded (1M on CI); W5 re-stamped.
- **P5-M44 (gate 12):** the measurement cells recorded and the representation selected on evidence; the metadata budget pin green.
- **P5-M45 (gate 12):** the 1..32-thread matrix recorded; the sharding decision evidence-driven either way.
- **P5-M46 (gate 12):** the k-sweep cells recorded; the two-tier ships only on evidence; rids_seen parity green.
- **P5-M47 (gate 12):** fresh artifacts stamped at the pushed head with the *_ns schema; both checkers fail with named schema errors, never KeyError; the 1M republish (M37 remainder) lands with this milestone.

## Honest-ledger template (pre-declared rows)

| Item | Milestone | Closure |
| --- | --- | --- |
| READ COMMITTED isolation | P5-M10 | SNAPSHOT only — kernel implements snapshot semantics; READ COMMITTED reopens on workload evidence |
| MCP transaction tools | P5-M10 | Deferred to P5-M11 — MCP request/response has no session affinity; the server-side handle registry belongs with the standalone server's connection model |
| Transaction reads of own staged writes | P5-M10 | Not visible — a transaction reads its pinned snapshot only (documented in the contract); reopens if a workload needs intra-transaction visibility |
| Mid-batch atomicity | P5-M10 | Rides the certified storage guarantee — the outcome row is in the same engine batch as the writes, and batch atomicity is the d05/ci-suite certified property |
| Non-transactional catalog entities (constraint/policy/model/user/role/tenant) | P5-M7 | Schema rows, not transactional writes — transactional when a milestone consumes them |
| Temporal/provenance index types | P5-M8 | Catalog schema-only until a workload profile shows the need (decl-shape rows land; no implementation) |
| Index batch APIs | P5-M8 | Closure row: the P4-M7 text batch path is the shipped cell preserved behind TextIndexAdapter (one Tantivy commit per batch); no new property-index batch API — in-memory per-op scalar upserts show no gain cell at current scale (rule 11, SE2-M25 discipline) |
| `aikoqld` / `aikoql` rename | P5-M11/12 | Ship 1.0 as `aikoql-mcp`; rename is a 1.0-cutover product decision with aliases (user-owned) |
| PG-wire protocol adapter | P5-M11 | Non-goal — reopens when a SQL-ecosystem deployment demands it |
| DISTINCT (ND-05 list item) | P5-M5 | No grammar clause was pre-declared in the ag list; P5-M6 shipped without it — reopens on workload evidence or a grammar proposal |
| Aggregate spill-to-disk / gate-7 RSS cell | P5-M5 | Strategy documented (partitioned hash spill on the group table, storage-v2 segment format); the env-gated cell rides with the W-suite sampler harness — no production memory claim until then |
| ORDER BY over `sum(age)`-style outputs | P5-M5 | Only `count` (COUNT(*) name) is grammar-referenceable; aliases/quoted identifiers would unlock the rest — reopens with the catalog (P5-M7) |
| CBO alternatives for traversal/join/search execution | P5-M9 | Exactly ONE executable choice exists in v1 (property-index-assisted scan); every other operator is cost-modeled with its v1 strategy — there is no second traversal/join/vector execution engine to choose between. Selection reopens when a second engine lands |
| Exec-time index re-verification | P5-M9 | The CBO verifies at optimize time (freshness watermark + clean verify gate the flag); the executor does not re-verify — EVENTUAL semantics by contract, the idx2-008 precedent |
| Per-rel-type fanout | P5-M9 | Type-level average degree — per-edge statistics reopens on workload evidence |
| M9 RED corrections | P5-M9 | cbo_a01 updates restate properties (kernel update semantics REPLACE the property map — only extensions carry forward); cbo003 fanout from distinct targets (the relationship index is an edge-set — identical (src, rel_type, dst) triples collapse); cbo010 uses the `AS_OF` token |
| PR6 hygiene finding (tracked node_modules/.pyd/Go-Java-TS SDKs) | review | VERIFIED FALSE — `git ls-files` shows no node_modules, no .pyd at head or ever added on the branch; the Go/Java/TS SDKs appear in the PR diff as DELETIONS (the P3-M9 decision), which the review read as additions; examples/hello-agent.ts is kept deliberately. No action taken |
| HNSW checkpoint pair torn-write | review | Mitigated at the maintainer level (tmp dir + COMPLETE-last + atomic rename — checkpoint_water refuses without COMPLETE); the residual gap is direct loaders not gated on the marker — a generation manifest lands with P5-M22 |
| Multi-model vector tombstones | review | Moot in the current data model — one KO carries ONE SemanticBlock embedding, so KOID-level tombstones are identity-correct; P5-M23 pins it (P1-18): ann005's distinct-(koid, model) map + the M23 docs row state the invariant |
| HNSW capacity hard bound | review | fast-hnsw capacity is a pre-allocation hint (the graph grows past it) — the claimed hard 10k bound is incorrect, but the >10k evidence cell is real and scheduled (P5-M23) |
| Query-level resource governance | review (P1-09/10) | The materialization honesty is now the PRODUCT contract: docs/server-contract.md §Query resource governance — no memory budget (full `Vec<KnowledgeObject>` materialization), spill is strategy-only, no row limit, timeout = response deadline, cooperative cancellation |
| O(1) cache eviction (intrusive list) | PR6 perf review (P0-04) | Ships as the SAFE lazy-deletion gen-heap (P5-M30: touch O(1), evict amortized O(log n), no unsafe) — the review's mandated intrusive doubly-linked list (unsafe, O(1) evict) stays the documented upgrade path, reopened only if a churn profile shows heap pops material |
| Memtable borrowed-key shortcut | PR6 perf review (P0-03) | No shortcut exists — `Borrow` cannot bridge a tuple key to a borrowed pair; the VersionChain restructure IS the fix (P5-M31), no auxiliary HashMap (the review's condition) |
| Sampled read instrumentation | PR6 R4 review (P2-03) | Read-trace 0=off is already zero-cost (P4-M5 delta-of-aggregates wrappers — the counters only count when armed). A sampled mode reopens AFTER the M45 contention matrix shows measurable multicore cache-line traffic — the review's own condition. No action until then |
| M35 keep reversal | PR6 R4 review (P1-01) | M35 kept the per-write scan_l0 on its cell — 345 ns/write = 0.7% at ~64 segments, regime-true. The review's O(segments) asymptote wins as the count grows: M40 SHIPPED 2026-09-22 — the O(1) authoritative counters replaced the per-write scan, scan_l0 stayed as the debug validator (debug_scan_l0), and the M35 scan_l0_calls/scan_l0_ns counters now count only validator runs (prof002 flipped to pin the zero: 100k writes → 0 scans in both regimes). Honest: the keep was cell-justified, the reversal is asymptote-justified |
| M38 WAL truncate race | PR6 R4 review (P0-01) | The split removes the append-vs-truncate mutual exclusion the state lock used to provide — pr6_004's acked-write-lost failure pinned it mid-milestone. Phase A captures the WAL length under the lock; phase C saves the interleaved tail, resets the file, re-appends it — replay sees exactly the frames no published segment covers. In-code ponytail ceiling: the tail copy is O(writes interleaved into B's I/O window); a manifest base-offset replaces it if that volume ever dominates |
| M39 SEGMENT crash windows moved into staging | PR6 R4 review (P0-02) | The merge's park marker rides the published file's parent (`publish_atomic_writer_staged`), and the merge now publishes into `.compact-staging-*` — so FAIL_AFTER_SEGMENT_WRITE/_FSYNC and after_segment park inside the staging dir, not the data dir. compact_crash, crash_injection ci001/ci002, and merge_chunk's crash-after-segment were re-pinned to the staged namespace: the window SEMANTICS are unchanged (pre-compact state governs; the reopen sweeps the residue) — the three suites caught the relocation on their own, no gap shipped. The LOCATION/MANIFEST/PUBLISH windows publish at real paths in phase C and did not move |
| M39 sv004/sv005 workspace flake | workspace regression | The first full-workspace run failed server_lifecycle sv004/sv005 (auth fail-closed, tenant scoping) with empty stdout — parallel-suite contention (both bind ports while other binaries run servers), not the v2 change: 13/13 green standalone against the same v2 build and green on the full re-run. Recorded, not silent |
| M41 suite-run one-offs (three) | suite context | Three distinct one-off failures across the consecutive M41 full-suite runs: tier_depth_answers_match_oracle (one assert, db_tiered_read), sfm004 (snapshot_matrix.rs:356 count assert), and a flush_lock_scope hang (~42 min, ~30 s CPU). Each green standalone repeatedly (2/2+5/5, 5/5, 3/3), and the whole suite green on the arbiter re-run — the sv004/005 parallel-suite class (the machine had been running suites ~8+ hours; thermal load the plausible aggravator). None of the three shapes is producible by M41's diff (a publish entry point + its pins): recorded, not silent |
