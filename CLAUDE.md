# CLAUDE.md

Agent guidance for this repo lives in **AGENTS.md** — read it first.
Non-negotiable: TDD RED-first; one milestone = one commit set; any commit
stream ends with the dogfood re-stamp of `docs/PR6-TDD-DISPOSITIONS.md` as
the tip (`python scripts/dogfood-review-loop.py full`); never push (user pushes).
